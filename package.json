{
  "name": "peerspan-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @peerspan/desktop",
    "dev:web": "npm run dev:web --workspace @peerspan/desktop",
    "build": "npm run build --workspace @peerspan/desktop",
    "build:web": "npm run build:web --workspace @peerspan/desktop",
    "test": "npm run test --workspace @peerspan/desktop",
    "typecheck": "npm run typecheck --workspace @peerspan/desktop"
  },
  "engines": {
    "node": ">=22.12"
  }
}
